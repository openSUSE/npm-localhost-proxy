{
  "name": "local_npm_registry",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "devDependencies": {
    "@types/jest": "^26.0.16",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "jest -c jest.config.js && jest -c jest-integration.config.js",
    "test-watch": "jest --watch",
    "test-coverage": "jest --coverage",
    "test-integration": "jest -c jest-integration.config.js --watch",
    "test-integration-coverage": "jest -c jest-integration.config.js --coverage"
  },
  "author": "SUSE LLC",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@types/ssri": "^7.1.0",
    "ssri": "^6.0.1"
  }
}
