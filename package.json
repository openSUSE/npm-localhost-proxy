{
  "name": "local_npm_registry",
  "version": "1.0.0",
  "description": "proxies NPM packages over localhost allowing non-network run of `npm install`",
  "private": "true",
  "directories": {
    "doc": "README.md"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/ssri": "^7.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.1.2"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "jest -c jest.config.js --coverage && jest -c jest-integration.config.js --coverage",
    "test-watch": "jest --watch",
    "test-integration": "jest -c jest-integration.config.js --watch"
  },
  "author": "SUSE LLC",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "ssri": "^10.0.1"
  }
}
